class FunctionsReu {
  FunctionsReu._();

  static DateTime agregarFecha(
    int year,
    int month,
    int day,
    int hour,
    int minute,
    int second,
  ) {
    return DateTime(
      year,
      month,
      day,
      hour,
      minute,
      second,
    );
  }
}
